#include<stdio.h>
int main()
{
	int n, a = 1, b = 1;
	scanf_s("%d", &n);
	while (a <= n)
	{
		a = a * b;
		b++;
	}
	printf("%d", a);
	if (a > 5);
	{
		printf("%d", a);
	}
	if (a <= 5);
	{
		printf("%d", b);
	}
	return 0;


}