#include<stdio.h>
int caculate(int n) {
	int i = 0;
	int ret = 1;
	for (i = 1; i <= n; i++) {
		ret = ret * i;
	}
	return ret;
}
int main(){ 
	int sum = 0;
	int a = 0;
	for (a = 1; a <= 10; a++) {


		sum = sum + caculate(a);

	}

	printf("%d", sum);
	return 0;
}