#include<stdio.h>
int main() {
	int n = 0;
	int i = 0;
	int ret = 1;
	int sum = 0;
	printf("请输入你要得出的阶乘的数字\n");
	scanf_s("%d", &n);
for(i=1;i<=n;i++)
{ 
	ret = ret * i;
	sum = sum + ret;
}
printf("输出的结果为\n%d", sum);
	return 0;
}